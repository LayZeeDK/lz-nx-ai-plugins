{
  "name": "lz-nx-rlm",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "plugins/lz-nx.rlm/scripts",
  "projectType": "library",
  "tags": ["type:plugin"],
  "// targets": "typecheck uses tsc -p (not --build) because noEmit is incompatible with --build mode",
  "targets": {
    "typecheck": {
      "command": "tsc -p {projectRoot}/tsconfig.json --noEmit"
    }
  }
}
