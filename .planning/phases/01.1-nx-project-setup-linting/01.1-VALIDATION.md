---
phase: 1.1
slug: nx-project-setup-linting
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-05
---

# Phase 1.1 -- Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.x |
| **Config file** | `tests/lz-nx.rlm/vitest.config.mjs` (existing, needs include pattern update) |
| **Quick run command** | `npm exec nx test lz-nx-rlm-test` |
| **Full suite command** | `npm exec nx run-many -t lint,typecheck,test` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm exec nx run-many -t lint,typecheck,test`
- **After every plan wave:** Run `npm exec nx run-many -t lint,typecheck,test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01-01 | 01 | 1 | DX-01 | smoke | `npm exec nx show project lz-nx-rlm --json` | N/A (CLI) | pending |
| 01-02 | 01 | 1 | DX-01 | smoke | `npm exec nx typecheck lz-nx-rlm` | N/A (CLI) | pending |
| 01-03 | 01 | 1 | DX-02 | smoke | `npm exec nx run-many -t lint` | N/A (CLI) | pending |
| 01-04 | 01 | 1 | DX-03 | unit | `npm exec nx test lz-nx-rlm-test` | 8 test files (.mjs -> .ts) | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] `plugins/lz-nx.rlm/project.json` -- defines lz-nx-rlm project
- [ ] `plugins/lz-nx.rlm/tsconfig.json` -- enables typecheck for .mjs
- [ ] `tests/lz-nx.rlm/tsconfig.json` -- enables typecheck for .ts tests
- [ ] `eslint.config.mjs` (root) -- enables lint for all projects
- [ ] Install `@nx/eslint-plugin`, `typescript-eslint`, `@typescript-eslint/parser`, `eslint-config-prettier`, `eslint-plugin-import-x`
- [ ] Convert 8 test files from `.test.mjs` to `.test.ts`
- [ ] Update `vitest.config.mjs` include pattern from `**/*.test.mjs` to `**/*.test.ts`
- [ ] Update `tests/lz-nx.rlm/project.json` to add `tags: ["type:test"]`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Plugin ships clean (no dev tooling in distributed folder) | DX-01/DX-02 | Verification requires inspecting file listing | Check `ls plugins/lz-nx.rlm/` has no eslint.config, vitest.config |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
